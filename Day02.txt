인공지능은 발전이 빠르다
git config --global user.name "내유저이름" : git 이라는 기능에서 (설치한 scm) config(설정)을 조정한다 
-- global (컴퓨터 전역설정)user라는 객체에 name이라는 속성에 내유저이름을 저장한다
git config --global user.email "내이메일" 

이것들을 script 명령어라고 합니다.

파이썬,노드 실행할때도 스크립트 명령어로 합니다.

스크립트 명령어는 마우스나 버튼 클릭하는것보다 많이 어렵지만
강력한 기능을 내포하고있습니다.

프로그래밍 언어는 많다

하지만 현재 주로 쓰이는건
 *자바스크립트
 *파이썬
 *c++ c#
 *자바

 옛날에는 프로그래밍 언어가 유료였다
 유일하게 자바만 무료로 제공해 줬다(2009년전까지)
 하지만 2009년에 oracle에 인수되고나서 자바는 안좋아짐
 발전이 멈춤
 문제는 2009년전까지 개발된 시스템들은 전부 자바

 한국에선 전자정부 프레임워크라는 국가 프로젝트가 진행 됨
 해외도 비슷하게 자바로 대형 솔루션 만드는 작업을 진행 했음
 하지만 2011년도부터 자바가 유료전환을 하고 굉장히 골치가 아파졌음
 그리고 다른 언어들이 전부 무료로 전환됨( c계열 언어, 파이썬, 자바스크립트)

 파이썬은 python software foundation(psf) 재단이 관리합니다.
 -그래서 쳬계적이지 못함( 패치에 따라 오류가 계속 발생함)

 자바스크립트(타입스크립트), c계열 언어 -마이크로소프트가 관리함

 구글은 flutter(Dart) 언어를 만들었는데, 반응이 별로임

 자바 스크립트를 설치함
 언어도 우리가 처음부터 안만들고
 라이브러리를 설치함

js는 자바스크립트 전용
다른언어 코드 안먹음
test1.js라는 파일을 만들고
test1.js 가 있는 경로에서 터미널을 열고 node test1.js했더니 뭔가 실행이됨
이뜻은, 언어가 정상적으로 작동한다는거에요

과제 집에서 git scm설치 자바스크립트 설치 vs코드 설치